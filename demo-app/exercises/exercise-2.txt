Exercise 2

1. Within CarTool, create an array of car objects and assign it to a variable. Each car object should have the following properties, create 2 cars...
    id: number
    make: String
    model: String
    year: number
    color: String
    price: number

2. Beneath the car tool header element, create/display a car table composed of a table and related-elements. For the table, display appropriate column headers. Display the array of car obejcts in the table.

3. Ensure it works.